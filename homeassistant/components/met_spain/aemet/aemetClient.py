"""Client for calls to AEMET (REST API)."""


class aemetClient:
    """Client for calls to AEMET (REST API)."""

    def __init__(self):
        """Initialize."""

    async def request(self, url):
        """REST API request."""
        # response_type="Model200"
        # collection_formats=None,
        # post_params=None,
        # files=None,
        # query_params=None,
        # body=None,
        # auth_settings = ["api_key"]
        # async_req=True,
        # method = "GET",
        # _preload_content=True,
        # _request_timeout=None
        # _return_http_data_only=None

        # header_params = {}
        # HTTP header `Accept`
        # header_params["Accept"] = self._aemetClient.select_header_accept(
        #    ["application/json"]
        # )
        # HTTP header `Content-Type`
        # header_params[
        #    "Content-Type"
        # ] = self._aemetClient.select_header_content_type(
        #    ["application/json"]
        # )

        # url = www...../api/" + resource_path

        return None
